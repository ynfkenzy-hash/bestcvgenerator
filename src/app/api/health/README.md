This folder already contains the `/api/health` route used by the platform to verify that the application and database are reachable.

No changes are required here for the BestCVGenerator Stage 1 UI prototype.
